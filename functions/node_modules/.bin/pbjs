#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../google-gax/node_modules/protobufjs/bin/pbjs" "$@"
  ret=$?
else 
  node  "$basedir/../google-gax/node_modules/protobufjs/bin/pbjs" "$@"
  ret=$?
fi
exit $ret
